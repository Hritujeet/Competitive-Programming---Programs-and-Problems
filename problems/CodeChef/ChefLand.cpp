#include <iostream>
#include <vector>
#include <algorithm>

#define REP(i, j, k) for (int i = j; i < k; i++)
#define v vector<float>
#define PB push_back

using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    return 0;
}